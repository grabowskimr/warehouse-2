export type TMenuItem = {
    label: string,
    url: string,
    iconComponent: any
}

export type TProduct = {
    name: string,
    index: string,
    supplier: string,
    quantity: number,
    quantityType: string,
    price: string,
    picture: string | null
}