export type TMenuItem = {
    label: string,
    url: string,
    iconComponent: any
}

export type TProduct = {
    name: string,
    quantity: number,
    price: string,
    picture: string | null
}